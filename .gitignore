.DS_Store
*.bak

# The global ~/.gitignore on some machines ignores `docs/`. A repository
# .gitignore takes precedence over global excludes, so this negation restores
# docs/ for this project, where the design spec must be versioned.
!docs/
